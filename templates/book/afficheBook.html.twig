<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List Books</title>
</head>
<body>
    <h1>List Books</h1>
    <table border="1">
        <tr>
            <th>title</th>
            <th>categorie</th>
            <th>publication date</th>
            <th>published</th>
            <th>author</th>
            <th>edit</th>
            <th>delete</th>
        </tr>
        {% for b in book %}
        <tr>
            <td>{{ b.title }}</td>
            <td>{{ b.category }}</td>
            <td>{{ b.publicationDate |date('Y-m-d H:i:s') }}</td>
            <td>{{ b.published }}</td>
            <td>{{ b.author.id }}</td>
            <td><a href="{{ path('edit_book', {'id': b.ref}) }}">Edit</a></td>
            <td><a href="{{ path('delete_book', {'id': b.ref}) }}">Delete</a></td>
            <td><a href="{{ path('show_book', {'id': b.ref}) }}">show</a></td>
            
        </tr>
        {% endfor %}
    </table>
    
    {% set compteur0 = 0 %}
    {% set compteur1 = 0 %}
    {% for b in book %}
    {% if b.published %}
    {% set compteur0 = compteur0 + 1 %}
    {% else %}
    {% set compteur1 = compteur1 + 1 %}
    {% endif %}
    {% endfor %}
    <h4>le nombre des livres publier est : {{ compteur0 }}</h4>
    <h4>le nombre des livres non publier est : {{ compteur1 }}</h4>
    
        <a href="{{ path('app_Addbook') }}">
        <button>Create New</button>
    </a>

</body>
</html>
